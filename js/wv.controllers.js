(function () {
    'use strict';
    
    /*
        This creates a new module / namespace for controllers for our application
        and this file will grow as we add functionality to this module (like routing)
    */
    angular.module('wv.controllers', []);
}());